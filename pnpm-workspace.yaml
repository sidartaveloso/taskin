packages:
  - packages/*
  - dev

allowBuilds:
  # postinstall do core-js so imprime mensagem de financiamento; nada a construir
  core-js: false
  esbuild: true
  vue-demi: true

overrides:
  esbuild@<=0.24.2: '>=0.25.0'
