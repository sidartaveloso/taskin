import { describe, it, expect } from 'vitest';
import Button from './Button.vue';

describe('Button', () => {
  it('should render', () => {
    // TODO: Add tests
    expect(Button).toBeDefined();
  });
});
