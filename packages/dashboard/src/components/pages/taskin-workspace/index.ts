/*
 * So o componente e os tipos. O `storytype normalize` gerou este indice
 * exportando tambem o mock e as stories, e o `App.vue` importa daqui: o bundle
 * do dashboard levaria `storybook/test` e os dados de exemplo para producao.
 */
export * from './TaskinWorkspace.types';
export { default } from './TaskinWorkspace.vue';
