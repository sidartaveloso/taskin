import { describe, expect, it } from 'vitest';
import TaskinEffectThoughtBubble from './TaskinEffectThoughtBubble.ts';

describe('TaskinEffectThoughtBubble', () => {
  it('should render', () => {
    // TODO: Add tests
    expect(TaskinEffectThoughtBubble).toBeDefined();
  });
});
