import { describe, expect, it } from 'vitest';
import TaskinEffectPhone from './TaskinEffectPhone.ts';

describe('TaskinEffectPhone', () => {
  it('should render', () => {
    // TODO: Add tests
    expect(TaskinEffectPhone).toBeDefined();
  });
});
