import { describe, it, expect } from 'vitest';
import TrackingControls from './TrackingControls.vue';

describe('TrackingControls', () => {
  it('should render', () => {
    // TODO: Add tests
    expect(TrackingControls).toBeDefined();
  });
});
