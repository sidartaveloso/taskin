import { describe, it, expect } from 'vitest';
import FaceTrackingDebug from './FaceTrackingDebug.vue';

describe('FaceTrackingDebug', () => {
  it('should render', () => {
    // TODO: Add tests
    expect(FaceTrackingDebug).toBeDefined();
  });
});
