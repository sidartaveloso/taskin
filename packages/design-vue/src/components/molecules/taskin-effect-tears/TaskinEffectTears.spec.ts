import { describe, expect, it } from 'vitest';
import TaskinEffectTears from './TaskinEffectTears.ts';

describe('TaskinEffectTears', () => {
  it('should render', () => {
    // TODO: Add tests
    expect(TaskinEffectTears).toBeDefined();
  });
});
