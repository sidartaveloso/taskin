import { describe, expect, it } from 'vitest';
import DayBar from './DayBar.vue';

describe('DayBar', () => {
  it('should render', () => {
    // TODO: Add tests
    expect(DayBar).toBeDefined();
  });
});
