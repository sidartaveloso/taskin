import { describe, it, expect } from 'vitest';
import TaskinArmWithPhone from './TaskinArmWithPhone.vue';

describe('TaskinArmWithPhone', () => {
  it('should render', () => {
    // TODO: Add tests
    expect(TaskinArmWithPhone).toBeDefined();
  });
});
