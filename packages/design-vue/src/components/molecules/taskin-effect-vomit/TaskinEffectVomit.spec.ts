import { describe, expect, it } from 'vitest';
import TaskinEffectVomit from './TaskinEffectVomit.ts';

describe('TaskinEffectVomit', () => {
  it('should render', () => {
    // TODO: Add tests
    expect(TaskinEffectVomit).toBeDefined();
  });
});
