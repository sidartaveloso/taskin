import { describe, expect, it } from 'vitest';
import TaskinMouth from './TaskinMouth.vue';

describe('TaskinMouth', () => {
  it('should render', () => {
    // TODO: Add tests
    expect(TaskinMouth).toBeDefined();
  });
});
