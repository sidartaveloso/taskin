import { describe, expect, it } from 'vitest';
import TaskinArms from './TaskinArms.vue';

describe('TaskinArms', () => {
  it('should render', () => {
    // TODO: Add tests
    expect(TaskinArms).toBeDefined();
  });
});
