import { describe, expect, it } from 'vitest';
import TaskinPhone from './TaskinPhone.vue';

describe('TaskinPhone', () => {
  it('should render', () => {
    // TODO: Add tests
    expect(TaskinPhone).toBeDefined();
  });
});
