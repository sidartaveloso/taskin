import { describe, expect, it } from 'vitest';
import Taskin from './Taskin.ts';

describe('Taskin', () => {
  it('should render', () => {
    // TODO: Add tests
    expect(Taskin).toBeDefined();
  });
});
