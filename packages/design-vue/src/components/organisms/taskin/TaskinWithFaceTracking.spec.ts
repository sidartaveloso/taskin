import { describe, it, expect } from 'vitest';
import TaskinWithFaceTracking from './TaskinWithFaceTracking.vue';

describe('TaskinWithFaceTracking', () => {
  it('should render', () => {
    // TODO: Add tests
    expect(TaskinWithFaceTracking).toBeDefined();
  });
});
