import { describe, it, expect } from 'vitest';
import PrioritizationPage from './PrioritizationPage.vue';

describe('PrioritizationPage', () => {
  it('should render', () => {
    // TODO: Add tests
    expect(PrioritizationPage).toBeDefined();
  });
});
