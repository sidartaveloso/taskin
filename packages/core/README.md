# @taskin/core

This package contains the core business logic for Taskin.

## Motivation

The motivation is to have a centralized package for all the core business logic, domain models, and services. This ensures that the business rules are consistent across different applications (CLI, API, etc.).

## Purpose

This package solves the problem of scattered business logic by providing a single source of truth for core functionalities.
