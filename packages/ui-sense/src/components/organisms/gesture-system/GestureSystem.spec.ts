import { describe, expect, it } from 'vitest';
import GestureSystem from './GestureSystem.vue';

describe('GestureSystem', () => {
  it('should render', () => {
    // TODO: Add tests
    expect(GestureSystem).toBeDefined();
  });
});
