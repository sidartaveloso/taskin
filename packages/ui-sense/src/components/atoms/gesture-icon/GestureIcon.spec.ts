import { describe, expect, it } from 'vitest';
import GestureIcon from './GestureIcon.vue';

describe('GestureIcon', () => {
  it('should render', () => {
    // TODO: Add tests
    expect(GestureIcon).toBeDefined();
  });
});
