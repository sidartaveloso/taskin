import { describe, expect, it } from 'vitest';
import WebcamVideo from './WebcamVideo.vue';

describe('WebcamVideo', () => {
  it('should render', () => {
    // TODO: Add tests
    expect(WebcamVideo).toBeDefined();
  });
});
