import { describe, expect, it } from 'vitest';
import GestureLegend from './GestureLegend.vue';

describe('GestureLegend', () => {
  it('should render', () => {
    // TODO: Add tests
    expect(GestureLegend).toBeDefined();
  });
});
