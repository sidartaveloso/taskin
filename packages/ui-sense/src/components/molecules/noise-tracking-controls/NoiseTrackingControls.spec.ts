import { describe, expect, it } from 'vitest';
import NoiseTrackingControls from './NoiseTrackingControls.vue';

describe('NoiseTrackingControls', () => {
  it('should render', () => {
    // TODO: Add tests
    expect(NoiseTrackingControls).toBeDefined();
  });
});
