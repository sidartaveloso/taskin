import { describe, expect, it } from 'vitest';
import GestureWizardCard from './GestureWizardCard.vue';

describe('GestureWizardCard', () => {
  it('should render', () => {
    // TODO: Add tests
    expect(GestureWizardCard).toBeDefined();
  });
});
