# @taskin/fs-task-provider

This package provides a file-system-based implementation of the `ITaskProvider` interface from `@taskin/task-manager`. It interacts with markdown files in the `TASKS` directory.
